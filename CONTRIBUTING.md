# 함께 만들기

"내 일에서 이게 제일 귀찮다"는 문제에서 시작합니다. 개발 경험은 필수가 아닙니다.

## 스킬 제안과 제작

1. [Issues](https://github.com/moduaiskill/modu/issues)에서 업무, 사용자, 기존의 어려움, 기대 효과를 공유합니다. 비슷한 제안도 확인합니다.
2. `templates/skill-template/`을 `skills/<분야>/<스킬명>/`에 복사합니다. 폴더명과 `name`을 같은 영문 소문자·숫자·하이픈 이름으로 바꿉니다.
3. 템플릿의 작성 안내를 실제 지침으로 대체합니다. [제작 규격](docs/skill-standard.md)을 참고합니다.
4. 가상 또는 공개 가능한 입력으로 직접 사용합니다. 입력, 출력, 확인한 모델과 날짜, 알려진 한계를 PR에 적고 결과물 캡처를 첨부합니다.
5. Pull Request에 사용자·사용 근거·효용을 함께 설명합니다. 1기 최종 피드백 담당은 이호준입니다. 검수 상태와 실제로 확인한 환경을 구분해 기록합니다.

GitHub가 익숙하지 않은 1기 참여자는 회의록에 안내된 카카오톡 채널을 통해 폴더와 캡처를 전달할 수 있습니다. 공개 참여는 Issues를 이용합니다.

## 작은 개선도 환영합니다

사용 후기, 잘못된 결과의 재현 예시, 설명 개선, 다른 모델에서의 동작 확인도 기여입니다. 개인정보나 기관의 비공개 자료 대신 가상 데이터를 사용합니다.

## 홈페이지에 소개하기

`assets/js/catalog.js`를 수정합니다. 제작 전에는 `planned` 상태를 유지합니다. 배포 파일이 준비되면 `shared`로 바꾸고 실제 `SKILL.md`와 ZIP 경로, 요구 환경, 소개 문단·작업 흐름·폴더 구성을 채웁니다. 공유 상태와 최종 검수 완료는 구분합니다. 새 항목에는 고유한 `id`를 사용하며, 스킬 상세 페이지는 `skill.html?id=<id>`로 열립니다.

스킬로 만든 결과물은 `examples/`에 파일을 두고 `assets/js/results.js`에 항목을 추가한 뒤, 스킬 항목의 `results`에 결과물 `id`를 연결합니다. 가상 데이터로 만든 예시는 `origin: "generated"`로 표시합니다. 기수 활동 기록은 `assets/js/cohorts.js`에서 갱신합니다.

스킬 수정 후 `python scripts/package_skills.py`로 ZIP도 갱신합니다. 이 스크립트는 현재 공유 중인 2개 스킬을 패키징하며 새 공유 스킬은 스크립트의 목록에도 추가합니다.

## 외부 자료 소개하기

[recommended/README.md](recommended/README.md)에 원본 주소, 작성 주체, 라이선스, 활용 목적과 확인 날짜를 남깁니다. 외부 자료를 자체 제작물이나 검수 완료 스킬로 표시하지 않습니다.

직접 작성한 기여물은 MIT 라이선스로 공유합니다. 타인의 자료는 사용 권한과 원래 라이선스를 확인합니다.
